# ============================================================
# 送迎ルート最適化システム v2 - requirements.txt
# Streamlit Community Cloud（無料枠）対応
# ============================================================

# --- コア（必須） ---
streamlit>=1.35.0
pandas>=2.0.0
openpyxl>=3.1.0          # Excel読み書き・2シート出力

# --- 地図表示（任意・なくても動作する） ---
folium>=0.16.0
streamlit-folium>=0.20.0

# --- VRP最適化（任意・なくてもグリーディで動作する） ---
# ortools>=9.8.0
# ※ Streamlit Community Cloud の無料枠では
#   OR-Tools のビルドに時間がかかる場合があります。
#   最初はコメントアウトのままデプロイし、
#   動作確認後に有効化することを推奨します。
